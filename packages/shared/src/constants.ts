export const DEFAULT_QUESTION_TIME_SECONDS = 20;
export const MAX_PLAYERS_PER_GAME = 50;
export const MAX_CHANNELS_PER_COMMUNITY = 5;
export const INVITE_TOKEN_EXPIRY_DAYS = 7;

/** When true, anyone can play trivia at /game without logging in. */
export const GUEST_PLAY_ENABLED = true;
